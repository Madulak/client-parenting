
const initialState = {
    isAuth: false,
    user: null,
}

const authReducer = (state = initialState, action) => {

    switch(action) {

        default:
            return state;
    }
}

export default authReducer;