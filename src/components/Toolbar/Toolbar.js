import classes from './Toolbar.module.css';

const Toolbar = () => {

    return (
        <div className={classes.Toolbar}>
            <h1>Toolbar</h1>
        </div>
    );
}

export default Toolbar;